name: Code quality & SAST & Dependency Scan

on: 
    # push:
    #     branches: [main]
    workflow_call:
    workflow_dispatch:

jobs:
    Code-quality-and-Dependency-Scan:
        runs-on: ${{matrix.os}}
        strategy:
            fail-fast: false
            matrix:
                os: ["macos-latest",  "ubuntu-latest"]
                # os: ["macos-latest", "windows-latest", "ubuntu-latest"]

        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: Setup Node.js 
              uses: actions/setup-node@v4 
              with:
                 node-version: 18 
                 cache: npm

            - name: Install Dependencies
              run: npm ci

            - name: Run ESLint 
              run: npm run lint

            - name: Install Semgrep
              run: pip install semgrep

            - name: Run SAST with Semgrep
              run: semgrep scan --config auto

            - name: Run dependency scan 
              run: npm audit --audit-level=high

            #For now just ignore the vulnerabilities cuz no developer

            # - name: Run dependency scan
            #   run: |
            #       CRITICAL=$(npm audit --json | node -e "
            #         let data = '';
            #         process.stdin.on('data', chunk => data += chunk);
            #         process.stdin.on('end', () => {
            #           const audit = JSON.parse(data);
            #           console.log(audit.metadata.vulnerabilities.critical);
            #       });
            #       ")

            #       echo "Critical vulnerabilities: $CRITICAL"

            #       if [ "$CRITICAL" -gt 5 ]; then
            #         echo "❌ Critical vulnerabilities are greater than 5."
            #         exit 1
            #       else
            #         echo "✅ Critical vulnerabilities are 5 or fewer."
            #       fi



